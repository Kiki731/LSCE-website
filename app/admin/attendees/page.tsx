import AttendeesManager from '@/components/admin/AttendeesManager'

export const metadata = { title: 'Attendees — LSCE Admin' }

export default function AttendeesPage() {
  return <AttendeesManager />
}
